import 'package:flutter/material.dart';

/// 应用颜色定义
class AppColors {
  AppColors._();

  // ============ 主色调 ============
  static const Color primary = Color(0xFF6C63FF);
  static const Color primaryLight = Color(0xFF9A95FF);
  static const Color primaryDark = Color(0xFF5046E5);

  // ============ 背景色 ============
  static const Color background = Color(0xFF1A1A2E);
  static const Color surface = Color(0xFF16213E);
  static const Color surfaceLight = Color(0xFF1F2B47);

  // ============ 文字色 ============
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFB0B0C3);
  static const Color textHint = Color(0xFF6B6B80);

  // ============ 状态色 ============
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFC107);
  static const Color error = Color(0xFFF44336);
  static const Color info = Color(0xFF2196F3);

  // ============ 复习评分色 ============
  static const Color feedbackAgain = Color(0xFFE53935);
  static const Color feedbackHard = Color(0xFFFFA726);
  static const Color feedbackEasy = Color(0xFF43A047);

  // ============ Drill 模式色 ============
  static const Color drillBackground = Color(0xFF000000);
  static const Color drillText = Color(0xFFFFFFFF);
  static const Color drillTextSecondary = Color(0xFF888888);
}
